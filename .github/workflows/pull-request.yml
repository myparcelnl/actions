name: 'Pull request'

on:
  pull_request:
    branches:
      - main

jobs:
  verify-cache:
    runs-on: ubuntu-22.04
    steps:
      - name: 'Checkout'
        uses: actions/checkout@v3

      - name: 'Verify Yarn cache'
        run: yarn install --check-cache
